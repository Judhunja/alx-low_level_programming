#include "main.h"
#include <stdio.h>
#include <stdlib.h>


/**
  * program prints its name, followed by a new line
  *
  *
  * Return: always zero (success)
  *
  */

int main (int argc, char *argv[])
{
	int i;

	for (i = 0; argc < i; i++)
	{
		printf ("%s\n",argv[i]);
		return (0);
	}
}
