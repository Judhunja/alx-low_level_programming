#include "main.h"
/**
 * _abs - returns the absolute value of an integer
 * @n: integer input
 * Return: integer converted to its absolute value
 */


int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
